#### QuickNotes Settings ####
# The editor to use; internal is kilo editor
editor internal
# The template to use (overriden by the -t option on the command line)
template memo
# The path of the directory to store the notes
notes ~/QuickNotes
# The default category (overriden by the -c option on the command line)
category unclassed
